Được. Dưới đây là **full bộ prompt theo từng step** cho `Week03_Data_Cleaning.html`.

Week03 trong roadmap tập trung vào **Data Cleaning & Quality**: profiling dữ liệu, missing values, deduplication, outlier handling, schema validation, và mindset “garbage in → garbage out”. Đây là tuần rất quan trọng vì nhiều pipeline hỏng không phải do code, mà do chất lượng dữ liệu đầu vào.
Format chuẩn vẫn giữ: hero, sidebar, 6 section/module, interactive quiz, practice, mini test, dark premium UI, consistency với Week00 family.

---

# 1) Prompt build cho Claude Extension / Claude Code

```text
Tôi đang xây dựng bộ tài liệu học Data Engineering dạng HTML tương tác.
Apply project rules from .claude.

Nguồn chính:
- Repo: ./data-engineer-handbook
- File roadmap local: DE_Roadmap_Expert_NHC.md
- File cấu trúc local: PROMPT_Build_DE_Tutorial_HTML.md
- File format mẫu local: Week00_Setup.html

Nguyên tắc chung:
- Output bằng tiếng Việt
- Giữ thuật ngữ kỹ thuật tiếng Anh khi phù hợp
- Dạy theo mindset của Data Engineer
- Practical, production-oriented, không generic
- Giữ consistency với family của Week00
- Mỗi module phải có đủ 6 section chuẩn:
  1. Menu Tổng Thể
  2. Mục Đích Module
  3. Giải Thích Chi Tiết
  4. Câu Hỏi Ôn Tập
  5. Tổng Kết
  6. Bài Tập Thực Hành
- HTML standalone, inline CSS/JS
- Dark premium UI
- Sidebar + hero + interactive quiz + practice + mini test cuối file

Use project rules from .claude.

Current task:
Create or strongly upgrade:
./Week03_Data_Cleaning.html

Goal:
Build a high-quality, portfolio-grade tutorial for:

Tuần 3 – Data Cleaning & Data Quality

Allowed sources only:
- ./Week03_Data_Cleaning.html
- ./data-engineer-handbook/
- ./DE_Roadmap_Expert_NHC.md
- ./PROMPT_Build_DE_Tutorial_HTML.md
- ./Week00_Setup.html
- ./.claude/

Do not use any other files.

WEEK 03 CORE MISSION:
This file must teach data cleaning as a core Data Engineering responsibility, not as a side task.

The learner should understand:
- vì sao dirty data phá hỏng dashboard, model, KPI, pipeline
- cách profile dữ liệu trước khi transform
- xử lý missing values có chủ đích
- deduplication đúng logic business
- phát hiện và xử lý outliers
- schema validation để chặn lỗi sớm
- build cleaning artifact có thể đưa vào portfolio

This tutorial must feel stronger than common “pandas cleaning tips” content.

MANDATORY MODULES:
- W3.01 – Tổng Quan & Tidy Data Principles
- W3.02 – Data Profiling: Hiểu Data Trước Khi Làm
- W3.03 – Xử Lý Missing Values
- W3.04 – Deduplication Strategies
- W3.05 – Outlier Detection & Treatment
- W3.06 – Schema Validation với pandera
- W3.07 – Artifact: week3_data_cleaning/
- W3.08 – Mini Test Tổng Kết

MANDATORY TOPICS TO COVER DEEPLY:

W3.01
- garbage in, garbage out
- cleaning vs business logic
- tidy data principles
- rows = observations
- columns = variables
- one table = one entity/process
- common dirty data symptoms
- why cleaning decisions affect downstream analytics
- week outcomes checklist

W3.02
- profile before transform
- row count
- null ratio
- unique values
- duplicates
- min/max
- invalid categories
- data types mismatch
- distribution checks
- sample suspicious rows
- pandas profiling style mindset (without depending only on tools)
- SQL profiling queries where useful
- anti-pattern:
  - transform blindly before understanding data

W3.03
- types of missing data
- truly missing vs unknown vs not applicable
- dropna vs fillna
- constant fill
- mean/median/mode fill
- forward fill / backward fill
- conditional fill
- preserving null intentionally
- audit columns for imputation if useful
- business trade-offs of each approach
- anti-pattern:
  - replacing all nulls with 0

W3.04
- exact duplicates vs business duplicates
- duplicate keys
- latest record wins
- ROW_NUMBER dedup pattern
- fuzzy duplicate concept (light intro)
- composite key duplicates
- dedup in pandas
- dedup in SQL
- keeping audit trail of removed rows
- anti-pattern:
  - deleting duplicates without rule definition

W3.05
- what is an outlier
- valid extreme vs data error
- IQR method
- Z-score concept
- percentile capping / winsorization
- remove vs cap vs flag
- domain-driven thresholds
- visual thinking for distributions
- anti-pattern:
  - deleting every large value automatically

W3.06
- why schema validation matters
- fail fast in pipelines
- required columns
- dtype checks
- nullable constraints
- range checks
- allowed categories
- uniqueness checks
- pandera basics
- validation report mindset
- where schema validation fits in ETL flow

W3.07
- build realistic portfolio artifact:
  week3_data_cleaning/
  ├── profile.py
  ├── clean.py
  ├── validate.py
  ├── sample_raw.csv
  ├── sample_clean.csv
  └── README.md
- explain each file clearly
- include believable code snippets
- README must explain:
  - data issues found
  - cleaning rules
  - validation rules
  - before/after impact
  - lessons learned
- must feel portfolio-ready

W3.08
- final mini test: 20 questions
- cover:
  - profiling
  - missing values
  - deduplication
  - outliers
  - schema validation
- support score grading + reset + result guidance

HTML / UX REQUIREMENTS:
- Follow Week00 family style
- Hero banner
- Badge row
- Fixed sidebar
- Main content area
- Active module switching
- Collapsible sections
- Terminal-style code blocks
- Tip / Warn / Note boxes
- Interactive quiz
- Practice tasks with hidden solutions
- Final mini test
- Responsive mobile

Required JS functions:
- showModule(idx, el)
- goNext(idx)
- goPrev(idx)
- toggleSection(header)
- answerQuiz(el, correct, explanation)
- toggleSolution(btn)
- gradeFinalTest()
- resetFinalTest()

QUIZ REQUIREMENTS:
Each module must contain exactly 10 review questions.
Each question must have:
- 4 options A/B/C/D
- correct answer
- explanation
Wrong answers must explain why wrong.

PRACTICE REQUIREMENTS:
Each module must have practical exercises.
Must include:
- profiling tasks
- cleaning tasks
- dedup tasks
- anomaly investigation tasks
- validation tasks
- hidden solution button:
  💡 Xem lời giải

TECHNICAL QUALITY RULES:
- no placeholders
- no “...” shortcuts
- every code block should be believable and runnable
- examples must support real data workflows
- do not turn this into generic pandas tricks page
- focus on reasoning + business impact + repeatable cleaning logic

CONTENT STYLE:
- mentor-like
- clear
- practical
- calm
- confident
- beginner friendly
- production mindset
- real-world examples
- explain simple first, then deepen
- compare trade-offs
- mention anti-patterns

Avoid:
- robotic wording
- shallow tips lists
- filler text
- toy examples without business context

BEFORE WRITING:
First briefly state:
1. Which Week03 scope you are using from roadmap
2. Which format rules you inherit from Week00
3. Which assumptions you are making for sample datasets and cleaning scenarios

Then output the full final content for:
./Week03_Data_Cleaning.html
```

---

# 2) Prompt review cho Antigravity

```text
Use project rules from .claude.

Review ./Week03_Data_Cleaning.html as a senior Data Engineering data-quality reviewer.

Your job is NOT to rewrite the file immediately.
Your job is to audit:
- technical depth
- teaching quality
- data quality realism
- production usefulness
- portfolio value
- consistency with Week00 family
- HTML learning product quality

Review deeply across these areas:

A. CONTENT QUALITY
- clarity
- correctness
- beginner friendliness
- progression of difficulty
- practical usefulness
- depth vs simplicity balance
- examples quality
- anti-pattern coverage
- trade-off thinking

B. DATA ENGINEERING RELEVANCE
Ensure Week03 teaches cleaning as a DE responsibility, not generic spreadsheet cleanup.
Check relevance of:
- profiling before transform
- repeatable cleaning rules
- pipeline quality gates
- dedup logic
- anomaly handling
- schema enforcement
- downstream analytics impact

C. PRODUCTION REALISM
Check whether the file covers:
- row count reconciliation
- null ratio monitoring
- duplicate business keys
- latest-row dedup logic
- preserving audit trail
- distinguishing valid extreme values vs errors
- schema drift
- fail fast validation
- before/after measurement
- avoiding silent bad fixes

D. TECHNICAL ACCURACY
Review whether explanations and code are strong enough for:
- pandas cleaning operations
- SQL profiling queries
- dedup with ROW_NUMBER
- outlier methods
- pandera validation
- realistic rule design

E. HTML LEARNING PRODUCT QUALITY
- module order
- learner flow
- practice quality
- quiz quality
- summary quality
- consistency with Week00 style
- portfolio feel
- whether W3.07 artifact looks serious and useful

F. WHAT IS MISSING
Actively search for high-value missing topics such as:
- row count reconciliation
- source vs target checks
- business-rule validation
- anomaly flags instead of deletion
- logging cleaning decisions
- data quality metrics
Only recommend additions if they materially improve quality.

OUTPUT FORMAT (STRICT):

# Week03 Review Report

## 1. Executive Summary
- score /10
- strongest parts
- biggest weaknesses
- priority level

## 2. What Already Strong

## 3. Critical Gaps

## 4. Module-by-Module Improvements

## 5. Production Upgrades Recommended

## 6. Teaching Upgrades Recommended

## 7. Portfolio Value Assessment

## 8. Top 10 Immediate Patches

## 9. Final Verdict
- keep as is
- patch lightly
- patch strongly
- rebuild parts

IMPORTANT RULES:
- Be critical, useful, and specific
- Avoid generic praise
- Focus on high-ROI improvements
- Respect existing strengths
- Output in Vietnamese
```

---

# 3) Prompt patch cho Claude Code sau review

```text
Use project rules from .claude.

Current task:
Upgrade ./Week03_Data_Cleaning.html based on the latest review.

Goal:
Keep the current strong foundation, preserve good UI and strong explanations, and patch weak areas so Week03 becomes a high-value, portfolio-grade Data Cleaning tutorial for Data Engineers.

Allowed sources only:
- ./Week03_Data_Cleaning.html
- ./data-engineer-handbook/
- ./DE_Roadmap_Expert_NHC.md
- ./PROMPT_Build_DE_Tutorial_HTML.md
- ./Week00_Setup.html
- ./.claude/

Do not use any other files.

IMPORTANT STRATEGY:
- Do NOT rewrite from scratch
- Do NOT blindly overwrite strong content
- Preserve strong explanations, examples, comparisons, and teaching flow
- Replace only weak, shallow, duplicated, outdated, or missing parts
- Final result must feel like one polished tutorial
- Keep Week00 family design and interaction quality
- Keep the file standalone HTML
- Keep Vietnamese explanations

PATCH OBJECTIVES:
Improve the file across:
- data quality technical depth
- teaching clarity
- production realism
- business impact thinking
- portfolio value
- artifact realism

PATCH PRIORITIES:
Ran command: `python dump_html.py Week03_Data_Cleaning.html Week03_text.txt`
Listed directory DE-Learning
Ran command: `python dump_html.py Week03_Data_Cleaning_Claude.html Week03_text.txt`
Viewed Week03_text.txt:1-800
Viewed Week03_text.txt:801-1339

# Week03 Review Report

## 1. Executive Summary
- **score /10:** 9.8/10. Khóa học về Data Cleaning có độ sát thương thực chiến cao nhất từng được review. Áp dụng hoàn hảo chuẩn kỹ sư (Defensive programming, Quarantine, Audit flag) thay vì tư duy Excel/pandas xóa dòng tùy tiện. Quy tắc 10 câu hỏi/module đã được chấp hành hoàn toàn.
- **strongest parts:** Tư duy `Flag + is_imputed` thay vì chép đè dữ liệu. Kỹ thuật Dedup `ROW_NUMBER()` xịn xò. Phân định rạch ròi cú pháp `PERCENTILE_CONT` (PostgreSQL) vs `MEDIAN` (Oracle 19c). Sự xuất hiện của tool `Pandera` thể hiện đẳng cấp Data Engineer hiện đại.
- **biggest weaknesses:** Chưa biểu diễn kỹ thuật Categorical Mapping (Lookup dictionary) cho chuẩn hóa chuỗi. Bỏ quên thư viện `ydata-profiling` cho Artifact Notebook. Logic Python `.drop_duplicates()` ở snippets tự viết chưa mapping chuẩn với logic ROW_NUMBER bằng cách Sort & Keep Last. 
- **priority level:** Low (Chỉ cần Update/điều chỉnh vặt một số đoạn code snippet, bản chất hệ thống đã cực kì xịn).

## 2. What Already Strong
- **Data Engineering Relevance:** Tuyệt vời. Mọi thao tác xử lý NULL (W3.03) hay Outliers (W3.05) đều đi kèm giải thích theo nghiệp vụ (VD: `amount=0` trong banking khác `NULL` thế nào). Liên kết rất tốt với kiến trúc Medallion (Silver layer).
- **Production Realism:** Rất Production-ready. Thay vì `DELETE` thủ công, handbook dạy dùng `ctid`/`ROWID` và cảnh báo nguyên lý Soft-delete qua `is_deleted` flag.
- **HTML & Quality:** Giao diện sắc nét, module order quá trơn tru (Identify -> Chiến lược từng loại -> SQL Profiling -> Validation Python -> Output 4 artifacts). Số lượng Quiz đúng chuẩn `.claude`.
- **Technical Accuracy:** Các câu SQL CTE chạy validation/audit báo cáo PASS/FAIL cực kì chặt chẽ.

## 3. Critical Gaps
- **Thiếu Lookup Mapping Code (W3.06):** Module Inconsistency (Tidy) dạy về `TRIM(UPPER())` và `unaccent()` để chuẩn hoá địa danh (Ví dụ `Hà Nội`). Đáng ra nên có 1 snippet demo kỹ thuật `LEFT JOIN city_mappings` hoặc `pandas.Series.map()` vì đây mới là bài toán Standardize thực tế ngốn nhiều thời gian nhất của DE (Lookup Translation).
- **Missed Automation ở Artifact 1 (W3.09):** Khuyên dùng Jupyter Notebook cho Profiling Report nhưng code mẫu chỉ chạy `df.describe()` và `sns.heatmap`. Ở đẳng cấp của W3, thêm 2 dòng library cực mạnh của Data Engineer là `ydata-profiling` (hoặc `sweetviz`) tự động sinh báo cáo HTML sẽ biến Artifact này mang dáng vẻ chuyên nghiệp hơn gấp bội phần.
- **Missmatched Python Dedup Logic:** Ở SQL bạn chỉ rất tốt cách `ORDER BY created_at DESC` rồi lọc `rn = 1`, nhưng sang đến W3.03 đoạn `# Python-equivalent handbook baseline` lại ghi `df.drop_duplicates()`. Dùng hàm này mà không pre-sort và `keep='last'` thì nó sẽ giữ dòng cũ và quăng đi dòng mới, đi ngược hệ tư tưởng của truy vấn SQL răn dạy. 

## 4. Module-by-Module Improvements
- **W3.03 Missing Values:** Cập nhật lại Code mẫu Python của comment `df = df.drop_duplicates()` thành `df.sort_values(['created_at']).drop_duplicates(subset=['transaction_id'], keep='last')` cho đồng bộ về tư duy Lấy Dữ Liệu Mới Nhất.
- **W3.06 Tidy Data:** Bổ sung SQL Code quy chuẩn dữ liệu dạng Text thông qua Dimension/Mapping Table (`LEFT JOIN ref_city_mapping`).
- **W3.07 SQL Profiling:** Nên thêm truy vấn Count Reconciliation (S2T check): tính hiệu lệnh lấy Tổng Record ở Table Source trừ đi (Tổng Record Đã Load Ở Table Target + Table Rác DLQ) để chắc chắn không xảy ra Silent Data Loss khi làm sạch.
- **W3.08 Validation:** Phải thêm dòng `Check.is_unique()` cho cột `transaction_id` trong Pandera Schema để test đúp lại kết quả của module W3.04.
- **W3.09 Artifacts:** Cải tiến Notebook Profiling bằng đoạn code `from ydata_profiling import ProfileReport`.

## 5. Production Upgrades Recommended
- **Dead Letter Queue (DLQ):** Ở W3.08 (Fail loudly), hãy rào thêm một câu giải thích đối với Production: Các dòng record vi phạm Rule (Critical) thay vì chỉ raise Warning lên console thì nên thiết kế ghi đè (INSERT) bắn vào một Table "Thùng Rác" (Transaction_DLQ / Quarantine table) để đội Operations có thể Re-process sau này.

## 6. Teaching Upgrades Recommended
- Giải thích hàm `scipy.stats.mstats.winsorize` trong System Python cho Module W3.05 để học viên dễ triển khai kỹ thuật ngắt trần (Winsorization) thay vì code tính Tukey's rule bằng tay tốn thời gian. 

## 7. Portfolio Value Assessment
- **Sức nặng tuyệt đối.** Việc yêu cầu Output là 4 file bao gồm Validation Schema, Profiling và Before/After Documentation biến Assignment đào tạo thành một Take-home Test phỏng vấn Senior level hoàn chỉnh. Recruiters sẽ thực sự ấn tượng với tư duy truy tìm Audit bằng "is_imputed", "is_invalid_date" của người tạo ra Github Repo (Artifact) này. 

## 8. Top 10 Immediate Patches
1. Update W3.09 Snippet: Thêm `ydata_profiling` cho báo cáo EDA Jupyter.
2. Update W3.03: Sửa script snippet `df.drop_duplicates()` thành `df.sort_values(..).drop_duplicates(keep='last')`.
3. Update W3.06: Thêm ví dụ SQL mapping/lookup để chuẩn hoá từ viết tắt (`HCM` thành `Hồ Chí Minh`).
4. Bổ sung `Check.is_unique` vào Pandera ở W3.08. 
5. Nêu khái niệm DLQ (Quarantine table) bên cạnh exception raise ở W3.08.
6. Cung cấp câu truy vấn Source-To-Target count reconciliation trong W3.07. 
7. Giới thiệu thư viện thư viện `scipy` ở W3.05 để xử lý Winsorize nhanh trên Python.
8. Tránh bỏ sót các bẫy null ở Oracle, bổ sung dòng nhắc nhở: Khi test Null ở Oracle đừng quên dùng `NVL` cover logic rỗng lồng nhau.
9. Giữ vững phong cách 10 câu Quiz cực hay và chuyên sâu này. Đừng đổi.
10. Sửa các lỗi copy-paste nhỏ trong code `REGEXP_REPLACE` nếu có phiên bản Python tương tác.

## 9. Final Verdict
- **patch lightly** (Nền móng vô cùng đẳng cấp, chỉ tinh chỉnh và bổ sung các thư viện/mảnh code python liên quan theo hướng Production hoàn đương).

MANDATORY EDITING RULES:
1. First identify which current sections should be preserved
2. Then identify which weak/missing areas must be upgraded
3. Merge improvements intelligently
4. Keep all HTML/CSS/JS interactions working
5. Preserve 6-section structure in every module
6. Preserve quizzes, practice, and mini test
7. Upgrade examples where needed
8. Avoid duplication
9. Keep tone mentor-like, practical, and production-oriented

COMMON HIGH-VALUE UPGRADE AREAS TO PRIORITIZE WHEN REVIEW REQUESTS THEM:
- strengthen garbage in / garbage out framing
- improve profiling before cleaning mindset
- improve missing-value trade-offs
- improve dedup business logic examples
- improve outlier handling nuance
- improve schema validation realism
- improve before/after measurement examples
- strengthen W3.07 artifact so it feels portfolio-ready
- update quiz and final test when new concepts are added

EXPECTED RESULT:
Update only:
./Week03_Data_Cleaning.html
```

---

# 4) Prompt final review ngắn cho Antigravity

```text
Review the patched ./Week03_Data_Cleaning.html.

Focus only on:
1. Did the patch solve the previous critical gaps?
2. Any new issues introduced?
3. Any remaining high-ROI improvements?
4. Final score /10
5. Ship now or one more patch?

Output in Vietnamese.
Be concise, critical, and decisive.
```

---

# 5) Prompt QA cuối cho Claude Code

```text
Use project rules from .claude.

Current task:
Perform final QA on ./Week03_Data_Cleaning.html

Check carefully:
- broken HTML structure
- broken CSS layout
- broken JS interactions
- sidebar navigation
- showModule / goNext / goPrev
- collapsible sections
- quiz logic
- solution toggle
- final test grading/reset
- duplicated content
- unfinished wording
- weak transitions between modules
- consistency with Week00 family
- every module still has the required 6-section structure
- every module still has exactly 10 review quiz questions
- final mini test still works
- code blocks still look coherent and believable
- overall polish, readability, and portfolio feel

Fix only final QA issues.
Do not rewrite strong content unless needed.
Return the final polished content for:
./Week03_Data_Cleaning.html only.
```

---

# 6) Cách dùng đúng cho Week03

Theo thứ tự:

### Bước 1

Dùng **Prompt build** cho Claude

### Bước 2

Lấy file output gửi sang Antigravity với **Prompt review**

### Bước 3

Paste review đó vào **Prompt patch** cho Claude Code

### Bước 4

Nếu patch lớn, gửi lại Antigravity bằng **Prompt final review ngắn**

### Bước 5

Chạy **Prompt QA cuối** trong Claude Code rồi chốt file

---

# 7) Mẹo riêng cho Week03

Week03 rất dễ bị viết thành “mẹo xử lý pandas”.
Bạn nên thêm 1 dòng này vào prompt build hoặc patch:

```text
Do not turn Week03 into a generic data cleaning tips page.
Teach cleaning as a repeatable Data Engineering discipline with profiling, rule-based fixes, validation, and measurable business impact.
```

Nếu bạn muốn, mình tiếp tục viết luôn **full bộ prompt theo từng step cho Week04_Pipeline_Design.html**.
